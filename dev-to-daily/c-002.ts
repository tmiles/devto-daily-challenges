
/**
 * Complete date: 
 */
export function stringDiamond(character: string, maxAmount: number): string {
  if(character.length != 1) {
    return null;
  }
  else {
    let retString = '';
    for(let i = 1; i <= maxAmount; i++) {
      // determine middle
    }
  }
}