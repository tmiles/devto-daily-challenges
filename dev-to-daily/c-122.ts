/**
 * https://dev.to/thepracticaldev/daily-challenge-122-clockwise-spirals-7pn
 */

export function clockwiseSpiral(grid: number) {
  if(grid < 1) { return []; }
  let arr = [];
  let curNum = 1;
  for(let i = 0; i < grid; i++) {
    
  }
}